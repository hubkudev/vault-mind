export interface IVault {
	id: string;
	name: string;
	desc: string;
	bannerURL: string;
	createdAt: string;
}

export interface IKnowledge {
	id: string;
	contentType: string;
	content: string;
	createdAt: string;
}
